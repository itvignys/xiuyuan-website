# 杭州秀源智能科技有限公司官网 - 部署指南

## 方案一：Vercel 部署（推荐，最简单）

**Vercel** 是业界最流行的静态网站托管平台，支持自动部署、全球 CDN、HTTPS 证书，完全免费。

### 步骤：

1. **注册/登录 Vercel**
   - 访问 https://vercel.com
   - 用 GitHub 账号一键登录（推荐）

2. **导入项目**
   - 点击 "Add New Project"
   - 选择 "Import Git Repository" 或直接用本地上传
   - 如果代码在 GitHub 上，直接授权导入即可

3. **本地上传方式（无 GitHub）**
   - 安装 Vercel CLI：
     ```bash
     npm i -g vercel
     ```
   - 进入项目目录：
     ```bash
     cd xiuyuan-website
     ```
   - 执行部署：
     ```bash
     vercel --prod
     ```
   - 按提示登录并确认，约 30 秒后获得线上链接

4. **自动部署**
   - 绑定 GitHub 仓库后，每次 `git push` 自动重新部署
   - 每次提交都会生成独立预览链接

5. **绑定自定义域名**
   - 在 Vercel Dashboard → Project Settings → Domains
   - 添加你的域名（如 `xiuyuan.tech`）
   - 按提示配置 DNS 解析即可

---

## 方案二：GitHub Pages 部署（免费，适合开源项目）

**GitHub Pages** 是 GitHub 提供的免费静态网站托管，适合已有 GitHub 账号的用户。

### 步骤：

1. **创建 GitHub 仓库**
   - 登录 https://github.com
   - 新建仓库，命名为 `xiuyuan-website`
   - 设置为 Public（免费）或 Private（Pro 用户）

2. **推送代码**
   ```bash
   cd xiuyuan-website
   git init
   git add .
   git commit -m "init: 秀源智能官网"
   git branch -M main
   git remote add origin https://github.com/你的用户名/xiuyuan-website.git
   git push -u origin main
   ```

3. **开启 GitHub Pages**
   - 进入仓库 → Settings → Pages
   - Source 选择 "Deploy from a branch"
   - Branch 选择 `main`，文件夹选择 `/ (root)`
   - 点击 Save，约 1-2 分钟后访问 `https://你的用户名.github.io/xiuyuan-website`

4. **绑定自定义域名**
   - 在 Pages 设置中添加 Custom domain
   - 在域名 DNS 添加 CNAME 记录指向 `你的用户名.github.io`

---

## 方案三：Cloudflare Pages 部署（推荐，国内访问快）

**Cloudflare Pages** 全球 CDN 加速，国内访问速度优于 Vercel，同样免费。

### 步骤：

1. **注册 Cloudflare**
   - 访问 https://dash.cloudflare.com
   - 注册账号并验证邮箱

2. **创建 Pages 项目**
   - 左侧菜单 → Workers & Pages → Create application → Pages
   - 选择 "Upload assets"（直接上传）或连接 Git

3. **上传部署**
   - 将 `xiuyuan-website` 文件夹打包为 zip
   - 拖拽上传，自动部署
   - 获得 `xxx.pages.dev` 链接

4. **绑定自定义域名**
   - 在 Pages 项目设置中添加自定义域名
   - Cloudflare 会自动处理 DNS 和 HTTPS

---

## 方案四：阿里云/腾讯云对象存储（国内备案首选）

如果已有国内域名且完成备案，推荐用国内云厂商的静态网站托管，访问速度最快。

### 阿里云 OSS 部署步骤：

1. **创建 OSS Bucket**
   - 登录阿里云控制台 → 对象存储 OSS
   - 创建 Bucket，地域选择靠近用户的地区
   - 权限设置为 "公共读"

2. **开启静态网站托管**
   - Bucket 设置 → 基础设置 → 静态页面
   - 默认首页设置为 `index.html`

3. **上传文件**
   - 进入 Bucket → 文件管理 → 上传
   - 上传 `index.html`、`styles.css`、`main.js` 及图片文件夹

4. **绑定自定义域名**
   - Bucket 设置 → 传输管理 → 域名管理
   - 添加已备案域名，开启 CDN 加速

### 腾讯云 COS 部署步骤：

1. **创建 COS Bucket**
   - 登录腾讯云控制台 → 对象存储
   - 创建存储桶，访问权限 "公有读私有写"

2. **开启静态网站**
   - 存储桶 → 基础配置 → 静态网站
   - 索引文档设为 `index.html`

3. **上传并绑定域名**
   - 上传文件到存储桶
   - 在 CDN 控制台绑定已备案域名

---

## 方案五：Netlify 部署（拖拽即部署）

**Netlify** 支持最简单的拖拽部署，无需命令行。

### 步骤：

1. **访问 https://app.netlify.com**
2. **拖拽部署**
   - 将 `xiuyuan-website` 文件夹直接拖拽到 Netlify 首页
   - 自动完成部署，立即获得链接
3. **或连接 Git**
   - 点击 "Add new site" → Import an existing project
   - 授权 GitHub/GitLab，选择仓库自动部署

---

## 各方案对比

| 平台 | 费用 | 国内速度 | 自定义域名 | 自动部署 | 难度 |
|------|------|----------|------------|----------|------|
| **Vercel** | 免费 | 一般 | 支持 | Git 自动 | ⭐ 简单 |
| **GitHub Pages** | 免费 | 一般 | 支持 | Git 自动 | ⭐ 简单 |
| **Cloudflare Pages** | 免费 | 快 | 支持 | Git 自动 | ⭐ 简单 |
| **阿里云 OSS** | 按流量 | 最快 | 需备案 | 手动上传 | ⭐⭐ 中等 |
| **腾讯云 COS** | 按流量 | 最快 | 需备案 | 手动上传 | ⭐⭐ 中等 |
| **Netlify** | 免费 | 一般 | 支持 | Git 自动 | ⭐ 简单 |

---

## 推荐选择

- **无备案域名 + 海外用户多** → Vercel 或 Cloudflare Pages
- **无备案域名 + 国内用户多** → Cloudflare Pages
- **已备案域名 + 纯国内业务** → 阿里云 OSS / 腾讯云 COS
- **不想用命令行** → Netlify 拖拽部署
- **代码已在 GitHub** → GitHub Pages（最省事）

---

## 部署前检查清单

- [ ] 确认所有图片资源路径正确（相对路径 `./images/xxx.png`）
- [ ] 确认 CSS/JS 引用路径正确
- [ ] 本地预览无报错
- [ ] 手机端显示正常
- [ ] 已准备自定义域名（可选）

---

## 常见问题

**Q: 部署后样式丢失？**
A: 检查 CSS 文件路径是否为相对路径 `./styles.css`，避免使用绝对路径。

**Q: 如何更新网站内容？**
A: 
- Git 部署：修改代码 → `git push`，自动重新部署
- 手动部署：重新上传文件到对应平台

**Q: 需要备案吗？**
A: 使用国内云厂商（阿里云/腾讯云）且绑定国内域名时需要备案。使用 Vercel/Cloudflare/GitHub Pages 不需要备案。

**Q: 如何配置 HTTPS？**
A: 所有主流平台都自动提供 HTTPS 证书，无需手动配置。
